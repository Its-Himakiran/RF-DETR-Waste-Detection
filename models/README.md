# Place model checkpoints here

Copy your two trained checkpoints into this folder and rename them exactly:

| Source file              | Rename to                |
|--------------------------|--------------------------|
| taco_v3_final_best.pth   | taco_rfdetr.pth          |
| multiwaste_v3_best.pth   | multiwaste_rfdetr.pth    |

Both are ~130 MB. They are excluded from git via .gitignore
(GitHub blocks files over 100 MB).
